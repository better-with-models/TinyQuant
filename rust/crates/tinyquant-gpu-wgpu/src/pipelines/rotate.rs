//! rotate.rs — rotate/inverse-rotate pipeline (Part B + C).
//!
//! Applies the pre-built random rotation matrix `R` to a batch of FP32
//! vectors on the GPU, and provides the transpose (inverse) pass for
//! decompression.  Full implementation follows in Phase 27 Part B.
